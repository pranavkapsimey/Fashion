PORT=5000
JWT_SECRET=super_secret_change_me_in_production
ADMIN_EMAIL=admin@fashionstore.com
ADMIN_PASSWORD=admin123

# Optional: set to a MongoDB Atlas connection string to use a persistent
# database instead of the in-memory one. Required for reliable hosting.
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/fashion-store
